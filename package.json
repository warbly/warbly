{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean:jest": "jest --clearCache",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "prettier": "^2.2.1"
  }
}
